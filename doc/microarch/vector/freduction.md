# Vector Floating-Point Reduction Unit

> ⚠️ **Disclaimer:** This document was generated or modified by an AI model. While every effort is made to ensure technical accuracy, the underlying source code and hardware RTL implementation remain the absolute source of truth. Use at your own risk.

> **Intended Audience:** Hardware Developers

The Vector Floating-Point Reduction Unit (`rvv_backend_freduction.sv`) is responsible for executing RISC-V vector floating-point reduction instructions (e.g., `vfredsum`, `vfredmax`, `vfredmin`). It iteratively accumulates scalar results across the elements of a vector register.

## Structural Interfaces

The module acts as a functional execution unit within the Vector Backend, interfacing directly with the Reservation Station (RS) and the Reorder Buffer (ROB):

- **Input (RS):** Receives decoded micro-operations (`uop`) of type `PMT_RDT_RS_t`. Uses `uop_valid` and `uop_ready` for handshaking.
- **Output (ROB):** Submits the final scalar result (`result`) of type `PU2ROB_t` back to the ROB. Uses `result_valid` and `result_ready` for handshaking.
- **Trap Handling:** Synchronizes with global pipeline flushes via the `trap_flush_rvv` signal, immediately terminating inflight calculations.

## Internal Structure & Execution Modules

The reduction unit leverages standard `fpnew` execution cores to perform the underlying mathematical operations. The internal pipeline depth for both components is configured to 3 cycles (`PipeLine = 3`).

1.  **FP Adder (`fpnew_fma_multi`):** Configured as an adder (`fpnew_pkg::ADD`). It is activated when `uop.uop_funct6.ari_funct6[2]` is low.
2.  **FP Min/Max (`fpnew_noncomp`):** Configured as a comparator (`fpnew_pkg::MINMAX`). It is activated when `uop.uop_funct6.ari_funct6[2]` is high.

## State Transitions & Latency

The unit processes vector elements serially, maintaining an internal accumulator loop driven by a state machine:

- **`IDLE` (0):** The unit waits for a valid operation. Upon receiving `uop_valid` and asserting `uop_ready` (while no flush is active), it transitions to `MATH` and sets the `busy` flag. It captures the initial `vs1` and `vs2` operands.
- **`MATH` (1):** The unit iterates over the vector elements. The loop is tracked by `vld_ctr`, which increments up to `VLEN/WORD_WIDTH`.
  - The module stalls the next iteration until the previous element's result is valid (`sub_result_vld`). It then feeds the output of the `fpnew` blocks back into the `vs1` operand for the subsequent calculation.
  - If `result_last_uop` is reached and the reduction is complete, it transitions back to `IDLE` and clears the `busy` flag.
  - If `trap_flush_rvv` asserts during the `MATH` state, the unit forcefully aborts, clears its `busy` state, and returns to `IDLE`.

## Masking & Exception Accumulation

- **Masking:** The unit checks the `vs2_type` from the micro-operation. If a specific element is not active (i.e., `BODY_ACTIVE`), it acts as a pass-through mask (`mask_cur`), preserving the current accumulator value without dispatching to the `fpnew` blocks.
- **Exceptions:** Floating-point exception flags (`sub_status`) generated by the `fpnew` cores (NV, DZ, OF, UF, NX) are logically OR'd with an internal `fpexp_reg` to accumulate exceptions across the entire vector iteration before writing back the final `result.fpexp` to the ROB.

## Interfaces

| Signal           | Direction | Type           | Description                                                 |
| :--------------- | :-------- | :------------- | :---------------------------------------------------------- |
| `clk`            | Input     | `logic`        | Global clock signal.                                        |
| `rst_n`          | Input     | `logic`        | Active-low reset signal.                                    |
| `uop_valid`      | Input     | `logic`        | Valid signal from the Reservation Station (RS).             |
| `uop_ready`      | Output    | `logic`        | Ready signal to the Reservation Station (RS).               |
| `uop`            | Input     | `PMT_RDT_RS_t` | Decoded micro-operation payload from RS.                    |
| `result_valid`   | Output    | `logic`        | Valid signal for the reduction result to the ROB.           |
| `result`         | Output    | `PU2ROB_t`     | Scalar reduction result payload back to the ROB.            |
| `result_ready`   | Input     | `logic`        | Ready signal from the Reorder Buffer (ROB).                 |
| `trap_flush_rvv` | Input     | `logic`        | Synchronous trap/flush signal to abort inflight operations. |



---

> **Traceability:** Generated by Gemini. Derived from upstream commit 7d6e35cff17732cad8f99b866333423ea1f4d750.
